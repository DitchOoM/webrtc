package com.ditchoom.webrtc.testsuite

/**
 * W7 placeholder — the published consumer harness (vnet, timeline engine, container control plane).
 * The real surface is `withWebRtcHarness { natType(); relayOnly(); impaired() }`. Placeholder only.
 */
public object WebRtcTestsuite {
    public const val MODULE: String = "webrtc-testsuite"
}
