# Normalize line endings: LF in the repo, checked out native per-platform.
* text=auto eol=lf

# Windows launcher stays CRLF.
*.bat text eol=crlf

# Binary assets — never normalized or diffed as text.
*.jar binary
gradle/wrapper/gradle-wrapper.jar binary
